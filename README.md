# Languager Bot 🤖

Telegram бот для изучения слов с чистой архитектурой и автоматическими бекапами.

## Возможности ✨

- 📝 Сохранение пар слов (слово + перевод)
- 🎲 Случайная пара для повторения
- 📅 История по дням с пагинацией
- 🔐 Защита паролем
- 💾 Автоматические бекапы PostgreSQL каждые 24 часа
- 🧹 Автоматическая очистка данных старше 60 дней
- 🐳 Docker Compose для развертывания одной командой

## Быстрый старт 🚀

### 1. Предварительные требования

- Docker и Docker Compose
- Telegram бот токен (получить у @BotFather)

### 2. Получить токен бота

1. Открой Telegram и найди [@BotFather](https://t.me/BotFather)
2. Отправь команду `/newbot`
3. Следуй инструкциям (придумай имя и username)
4. Скопируй полученный токен (вида `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 3. Настройка проекта

```bash
# Клонируй или скачай проект
cd LanguagerEN2

# Создай .env файл из примера
cp .env.example .env

# Отредактируй .env файл
nano .env  # или любым другим редактором
```

**Обязательно заполни в .env:**
- `BOT_TOKEN` - токен от BotFather
- `BOT_PASSWORD` - пароль для доступа к боту (придумай сам)
- `DB_PASSWORD` - пароль для PostgreSQL (придумай надёжный)

### 4. Запуск

```bash
# Запусти всё одной командой
make up

# Или без Makefile:
docker-compose up -d --build
```

Готово! Бот запущен и работает 🎉

### 5. Проверка

```bash
# Посмотреть логи бота
make logs

# Или:
docker-compose logs -f bot

# Проверить все контейнеры
make ps
```

## Использование бота 📱

### Первый запуск

1. Найди своего бота в Telegram (по username который создавал)
2. Отправь `/start`
3. Введи пароль (который указал в .env файле)

### Добавление слов

Просто отправь боту английское слово (или русское):

```
hello
```

Бот ответит "Жду перевод", отправь перевод:

```
привет
```

Готово! Слово сохранено ✅

### Главное меню

Команда `/start` открывает главное меню с кнопками:

- **📅 Посмотреть дни** - история по дням (последние 60 дней, по 7 дней на страницу)
- **🎲 Случайная пара** - случайное слово с переводом для повторения

### Отмена

Если случайно начал вводить слово - нажми кнопку **❌ Отменить**

## Управление 🛠️

### Makefile команды

```bash
make up          # Запустить проект
make down        # Остановить проект
make logs        # Смотреть логи бота
make logs-all    # Смотреть логи всех сервисов
make restart     # Перезапустить бота
make backup      # Создать ручной бекап
make db          # Подключиться к PostgreSQL
make clean       # Удалить всё (включая данные!)
make ps          # Показать статус контейнеров
```

### Без Makefile

```bash
docker-compose up -d --build              # Запуск
docker-compose down                        # Остановка
docker-compose logs -f bot                 # Логи
docker-compose restart bot                 # Перезапуск
docker-compose exec postgres psql -U languager -d languager  # БД
```

## Архитектура 🏗️

Проект использует **Clean Architecture** для простого масштабирования:

```
Handler → Service → Repository → Database
   ↓         ↓          ↓
Telegram  Логика   PostgreSQL
```

Подробнее: [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)

## Структура проекта 📁

```
LanguagerEN2/
├── cmd/bot/                    # Главный файл приложения
├── internal/
│   ├── config/                # Конфигурация
│   ├── domain/                # Модели данных
│   ├── repository/            # Работа с БД
│   ├── service/               # Бизнес-логика
│   ├── handler/               # Telegram обработчики
│   └── middleware/            # Middleware
├── migrations/                # SQL миграции
├── scripts/                   # Скрипты (бекапы)
├── docker-compose.yml         # Оркестрация
├── Dockerfile                 # Сборка бота
└── Makefile                   # Удобные команды
```

## Бекапы 💾

### Автоматические бекапы

Бекапы создаются **каждые 24 часа** автоматически.

Хранятся в директории `backups/` в формате:
```
backup_20241212_143022.sql
```

По умолчанию хранятся **последние 30 бекапов** (2 месяца).

### Изменить количество бекапов

В `.env` файле:
```bash
BACKUP_RETENTION_DAYS=60  # Хранить 60 последних бекапов
```

### Ручной бекап

```bash
make backup
```

### Восстановление из бекапа

```bash
# 1. Остановить бота
make down

# 2. Запустить только PostgreSQL
docker-compose up -d postgres

# 3. Восстановить из бекапа
cat backups/backup_20241212_143022.sql | docker-compose exec -T postgres psql -U languager -d languager

# 4. Запустить всё снова
make up
```

## Переменные окружения ⚙️

| Переменная | Описание | Пример |
|-----------|----------|--------|
| `BOT_TOKEN` | Токен Telegram бота | `123456789:ABCdef...` |
| `BOT_PASSWORD` | Пароль для доступа к боту | `my_secret_pass` |
| `DB_HOST` | Хост PostgreSQL | `postgres` |
| `DB_PORT` | Порт PostgreSQL | `5432` |
| `DB_NAME` | Имя базы данных | `languager` |
| `DB_USER` | Пользователь БД | `languager` |
| `DB_PASSWORD` | Пароль БД | `strong_password` |
| `BACKUP_RETENTION_DAYS` | Сколько бекапов хранить | `30` |

## Особенности 🎯

- **Clean Architecture** - легко масштабировать и добавлять фичи
- **Graceful Shutdown** - корректное завершение при остановке
- **Автомиграции БД** - миграции применяются автоматически при старте
- **Структурированное логирование** - JSON логи для парсинга
- **State Machine** - управление состоянием пользователя
- **Connection Pooling** - оптимизация работы с БД
- **Автоочистка** - данные старше 60 дней удаляются автоматически

## Разработка 🔧

### Добавление новой фичи

1. Создай метод в `repository` (работа с БД)
2. Добавь логику в `service` (бизнес-правила)
3. Создай обработчик в `handler` (Telegram)
4. Зарегистрируй в `RegisterHandlers()`

Подробнее: [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)

### Локальная разработка

```bash
# Запустить PostgreSQL отдельно
docker-compose up -d postgres

# Получить connection string
echo "postgresql://languager:yourpassword@localhost:5434/languager"

# Запустить бота локально (нужен Go установлен)
go run cmd/bot/main.go
```

## Troubleshooting 🔍

### Бот не отвечает

```bash
# Проверить логи
make logs

# Проверить что контейнеры запущены
make ps

# Перезапустить
make restart
```

### Ошибки подключения к БД

```bash
# Проверить что PostgreSQL запущен
docker-compose ps postgres

# Проверить логи PostgreSQL
docker-compose logs postgres

# Попробовать подключиться вручную
make db
```

### Миграции не применяются

```bash
# Зайти в контейнер бота
docker-compose exec bot sh

# Проверить что миграции на месте
ls -la /app/migrations
```

### Бекапы не создаются

```bash
# Проверить логи бекапа
docker-compose logs backup

# Проверить права на директорию
ls -la backups/
```

## Производительность 📊

- **PostgreSQL индексы** для быстрых запросов
- **Connection pooling** (25 max connections, 5 idle)
- **Эффективная пагинация** (7 дней на страницу)
- **Автоочистка старых данных** (60 дней retention)

## Безопасность 🔐

- Пароль для доступа к боту
- Секреты в .env файле (не коммитятся)
- PostgreSQL с паролем
- Бот работает от non-root пользователя
- Volume для безопасного хранения данных

## Лицензия 📄

MIT License - делай что хочешь 😊

## Контакты и поддержка 💬

Если есть вопросы или нашёл баг - создай Issue в репозитории.

---

**Приятного изучения слов!** 🎓✨

